// Copywrite 2019 The Kegcoin Gold devs (rmove this and ill consume you)

const string max_peer_polls peerCount() // Max amount of peers to poll forblock height (set to peerCount() to poll every conected peer)
const uint64_t poll_timeout 100 // Time (in secconds) before we drop the conection to peer when reciving no data
const uint64_t coin_rpc_port 4057
